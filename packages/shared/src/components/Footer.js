import React from "react";

export const Footer = () => (
  <footer style={{ marginTop: 20, padding: 12, color: "#666", fontSize: 13 }}>
    © {new Date().getFullYear()} Add items to cart
  </footer>
);
